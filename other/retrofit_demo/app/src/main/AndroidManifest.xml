<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo_retrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.food_bill.showfood_data"></activity>
        <activity android:name="com.example.food_bill.food_bill" />
        <activity android:name="com.example.Search_Restaurant.restaurant_all_data" />
        <activity android:name="com.example.Search_Restaurant.all_restaurant" />
        <activity android:name="com.example.food.Food_detail" />
        <activity android:name=".retaurant_data" />
        <activity android:name=".restaurant" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>